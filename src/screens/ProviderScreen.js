import React from "react";
import { Text, StyleSheet } from "react-native";

const ProviderScreen = () => {
  return <Text style={styles.text}>PROVIDER MAIN PAGE</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default ProviderScreen;


